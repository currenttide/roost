<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key><string>Roost</string>
    <key>CFBundleDisplayName</key><string>Roost</string>
    <key>CFBundleIdentifier</key><string>com.roost.mac</string>
    <!-- Version is single-sourced from the repo's pyproject.toml
         ([project].version — the same number the control plane self-reports as
         __version__). scripts/build.sh stamps both keys at assembly time; the
         0.0.0 here is a deliberate "unstamped dev copy" placeholder, never a
         release number. -->
    <key>CFBundleVersion</key><string>0.0.0</string>
    <key>CFBundleShortVersionString</key><string>0.0.0</string>
    <key>CFBundlePackageType</key><string>APPL</string>
    <key>CFBundleExecutable</key><string>RoostMac</string>
    <key>LSMinimumSystemVersion</key><string>14.0</string>
    <!-- menu bar app: no Dock icon by default (a setting flips activation policy) -->
    <key>LSUIElement</key><true/>
    <key>NSHighResolutionCapable</key><true/>
    <!-- Plain-HTTP control planes are common on LANs/Tailscale (DESIGN.md §6).
         The app warns in Settings instead of blocking. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key><true/>
        <key>NSAllowsLocalNetworking</key><true/>
    </dict>
    <key>NSHumanReadableCopyright</key><string>Roost — open source fleet orchestrator</string>
</dict>
</plist>
